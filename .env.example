# Server Configuration
PORT=3000
NODE_ENV=development

# JWT Authentication
JWT_SECRET=your-secret-key-change-in-production

# PostgreSQL Database Configuration
DATABASE_URL=postgresql://username:password@localhost:5432/polsia

# For Render PostgreSQL (will be auto-filled by Render)
# DATABASE_URL=<your-render-postgres-url>

# Slack Notifications
SLACK_NOTIFICATIONS_ENABLED=true
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/YOUR/WEBHOOK/URL

# Anthropic Claude Agent SDK
ANTHROPIC_API_KEY=sk-ant-your-api-key-here

# OpenAI API Key (for Sentry MCP AI-powered search tools)
# Required for search_issues and search_events in Sentry MCP
OPENAI_API_KEY=sk-proj-your-openai-api-key-here

# GitHub OAuth Configuration
# Create OAuth App at: https://github.com/settings/developers
GITHUB_CLIENT_ID=your-github-client-id
GITHUB_CLIENT_SECRET=your-github-client-secret
GITHUB_CALLBACK_URL=http://localhost:3000/api/auth/github/callback
FRONTEND_URL=http://localhost:5173

# Gmail OAuth Configuration
# Create OAuth App at: https://console.cloud.google.com/apis/credentials
# 1. Create a new project or select existing
# 2. Enable Gmail API: https://console.cloud.google.com/apis/library/gmail.googleapis.com
# 3. Create OAuth 2.0 Client ID (Web application)
# 4. Add authorized redirect URI: http://localhost:3000/api/auth/gmail/callback
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_CALLBACK_URL=http://localhost:3000/api/auth/gmail/callback

# Token Encryption (64 hex characters = 32 bytes)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ENCRYPTION_KEY=your-64-character-hex-encryption-key-here

# Late.dev Social Media Management
# Get your API key from: https://getlate.dev/dashboard/api-keys
LATE_API_KEY=your-late-api-key-here
LATE_API_URL=https://getlate.dev/api

# Instagram OAuth (via Late.dev)
INSTAGRAM_CALLBACK_URL=http://localhost:3000/api/auth/instagram/callback

# Meta (Facebook) Ads OAuth Configuration
# Create app at: https://developers.facebook.com/apps/
# See setup guide: docs/meta-ads-setup.md
# 1. Create a Meta App (Business type)
# 2. Add Facebook Login product
# 3. Configure Valid OAuth Redirect URIs
# 4. Request permissions: ads_read, ads_management, business_management, pages_show_list, pages_read_engagement
META_APP_ID=your-meta-app-id
META_APP_SECRET=your-meta-app-secret
META_CALLBACK_URL=http://localhost:3000/api/auth/meta-ads/callback

# Sentry OAuth Configuration (for error monitoring)
# Create integration at: https://sentry.io/settings/account/api/applications/
# 1. Go to User Settings > API Applications
# 2. Click "Create New Application"
# 3. Add Redirect URI: http://localhost:3000/api/auth/sentry/callback
# 4. Note: Tokens expire every 8 hours and require refresh
SENTRY_CLIENT_ID=your-sentry-client-id
SENTRY_CLIENT_SECRET=your-sentry-client-secret
SENTRY_CALLBACK_URL=http://localhost:3000/api/auth/sentry/callback
SENTRY_BASE_URL=https://sentry.io

# Cloudflare R2 Storage Configuration
# Enable/disable R2 storage (set to 'true' to enable)
USE_R2=false
# Get these from your Cloudflare dashboard: https://dash.cloudflare.com/
# R2 > Manage R2 API Tokens
R2_ENDPOINT=https://your-account-id.r2.cloudflarestorage.com
R2_ACCESS_KEY_ID=your-r2-access-key-id
R2_SECRET_ACCESS_KEY=your-r2-secret-access-key
R2_BUCKET_NAME=your-bucket-name
# Public URL for accessing uploaded files (depends on your bucket's public access settings)
# For development, you can use the auto-generated R2.dev subdomain
R2_PUBLIC_URL_BASE_DEV=https://your-bucket.r2.dev
# For production, use your custom domain if configured
R2_PUBLIC_URL_BASE=https://media.yourdomain.com

# Fal.ai AI Content Generation
# Get your API key from: https://fal.ai/dashboard/keys
FAL_API_KEY=your-fal-api-key-here

# App Store Connect API (per-user integration, configured in UI)
# No environment variables required - credentials stored encrypted per user
# Users configure via Connections page in UI with:
#   - Key ID (10 characters)
#   - Issuer ID (UUID format)
#   - Private Key (from .p8 file)
# See: https://developer.apple.com/documentation/appstoreconnectapi
